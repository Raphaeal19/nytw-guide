# LLM backend: "ollama" (local, default) or "anthropic" (cloud fallback)
SYNTHESIS_BACKEND=ollama
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-haiku-4-5-20251001

OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:32b

# Scraping APIs
# Optional — leave blank to use DuckDuckGo (free) instead
SERPER_API_KEY=

# LinkedIn accounts (account rotation — disable 2FA on each)
# Format: email1:pass1,email2:pass2
LINKEDIN_ACCOUNTS=you@gmail.com:yourpass

# Pi connection (use http://localhost:8000 during local dev)
PI_URL=http://pi.local:8000
PI_API_SECRET=changeme

# Personal (used in outreach drafts)
MY_NAME=Ayush Pathak
